import { MetricNamesPipe } from './metric-names.pipe';

describe('MetricNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
